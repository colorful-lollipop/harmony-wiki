# 安全风险评审

## 评审范围

本评审覆盖 `arkcompiler/ets_frontend` 仓库的核心编译组件：
- `es2panda/` - JS/TS 编译器
- `ets2panda/` - ETS 编译器
- `merge_abc/` - ABC 文件合并
- `arkguard/` - 代码保护

**评审依据**: 代码静态分析 + 架构审查

## 威胁模型

### 信任边界

```
┌─────────────────────────────────────────────────┐
│              不可信输入边界                       │
│  ┌───────────────────────────────────────────┐  │
│  │  用户代码文件 (.js/.ts/.ets)               │  │
│  │  命令行参数                               │  │
│  │  外部配置文件                             │  │
│  └─────────────────┬─────────────────────────┘  │
│                    ▼                           │
│  ┌───────────────────────────────────────────┐  │
│  │  ets_frontend 编译器                       │  │
│  │  (本项目, 信任边界内)                      │  │
│  └─────────────────┬─────────────────────────┘  │
│                    ▼                           │
│  ┌───────────────────────────────────────────┐  │
│  │  ARK Runtime                               │  │
│  │  (运行时, 后续处理)                         │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

### 攻击面分析

| 输入类型 | 处理模块 | 风险等级 |
|----------|----------|----------|
| 源代码文件 | parser/lexer | 中 |
| 命令行参数 | es2panda.cpp | 中 |
| 模块依赖路径 | driver | 低 |
| 输出文件路径 | ir/output | 低 |

## 输入校验

### 文件路径处理

**当前实现**: 使用标准 C++ 文件操作 API

**潜在风险**:
- 路径遍历攻击 (../ 注入)
- 符号链接跟随
- 特殊字符处理

**建议**:
- 实现路径规范化 (realpath)
- 检查路径前缀是否在允许范围内
- 禁用符号链接或验证链接目标

### 命令行参数

**当前实现**: 参数解析器处理用户输入

**检查项**:
- [ ] `--output` 路径验证
- [ ] `--opt-level` 范围检查
- [ ] `--thread` 数量限制

## 已识别风险点

### 风险 1: 潜在的路径遍历 (待确认)

**位置**: TODO - 需要代码审查确认

**描述**: 如果输出文件路径未正确验证，攻击者可能通过路径遍历写入任意位置。

**触发条件**:
```
./es2abc --output /etc/passwd input.js
```

**影响**: 任意文件写入，可能导致权限提升

**修复建议**:
```cpp
// 输出前验证路径
std::string resolved = realpath(outputPath.c_str(), nullptr);
if (!IsPathInAllowedDir(resolved)) {
    throw SecurityException("Path outside allowed directory");
}
```

### 风险 2: 资源耗尽 (低风险)

**描述**: 大文件或复杂语法可能导致内存耗尽

**触发条件**:
- 极大源文件 (GB 级别)
- 深度嵌套语法
- 大量模块依赖

**缓解措施**:
- 编译器使用内存池管理
- 建议运行时内存限制

### 风险 3: 整数溢出 (低风险)

**位置**: 需代码审查确认

**描述**: 长度/大小计算可能存在整数溢出

**建议**: 使用安全的整数运算库或显式检查

## 安全最佳实践

### 已采用的安全措施

1. **C++17 标准**: 使用现代 C++ 特性
2. **内存池管理**: 避免 raw new/delete
3. **严格编译选项**: `-Wall -Wextra -Werror`

### 建议增强

| 项目 | 当前状态 | 建议 |
|------|----------|------|
| 输入路径验证 | 基本 | 增加路径规范化 |
| 资源限制 | 无 | 添加文件大小限制 |
| 错误处理 | 返回码 | 增强错误上下文 |
| 日志审计 | 基本 | 记录敏感操作 |

## 运行时 vs 构建时安全

**注意**: ets_frontend 是构建时工具，主要安全考量在：
- 输入验证
- 资源限制
- 输出安全

运行时安全由 `arkcompiler_ets_runtime` 负责。

## 相关链接

- [OpenHarmony 安全指南](https://gitee.com/openharmony/docs/blob/master/zh-cn/security/README.md)
- [ARK Runtime 安全说明](docs/Security.md)
